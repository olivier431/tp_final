<UserControl x:Class="tp_final.Views.AlbumView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:tp_final.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Menu VerticalAlignment="Top" Height="20" Grid.Row="0">
            <Separator/>
            <MenuItem Header="Playlist" Command="{Binding GoToMainPlayerCommand}"/>
            <Separator/>
            <MenuItem Header="Morceaux"/>
            <Separator/>
            <MenuItem Header="Album"/>
            <Separator/>
            <Separator/>
            <MenuItem Header="Admin" Command="{Binding GoToAdminCommand}"/>
            <Separator/>
            <Separator/>
            <Separator/>
            <Separator/>
            <Separator/>
            <Separator/>
            <Separator/>
            <Separator/>
            <Separator/>
            <MenuItem Header="Logout" HorizontalAlignment="Right" Command="{Binding GoToLogoutCommand}"/>
        </Menu>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <!--<ListBox ItemsSource="{Binding PlaylistViewSource}" Margin="10,10,10,10" Height="Auto" Width="AUTO" HorizontalAlignment="Stretch" Grid.Column="0">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">-->
            <ListView Name="DisplayList" ItemsSource="{Binding AlbumlistViewSource}" IsSynchronizedWithCurrentItem="True">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Id" Width="auto" DisplayMemberBinding="{Binding id}" />
                        <GridViewColumn Header="Title" Width="auto" DisplayMemberBinding="{Binding title}" />
                        <GridViewColumn Header="count" Width="auto" DisplayMemberBinding="{Binding count}" />
                        <GridViewColumn Header="Length" Width="auto" DisplayMemberBinding="{Binding length}" />
                    </GridView>
                </ListView.View>
                <!--<ListView.GroupStyle>
                                    <GroupStyle>
                                        <GroupStyle.ContainerStyle>
                                            <Style TargetType="{x:Type GroupItem}">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate>
                                                            <Expander IsExpanded="True">
                                                                <Expander.Header>
                                                                    <StackPanel Orientation="Horizontal">
                                                                        <TextBlock Text="{Binding title}" FontWeight="Bold" Foreground="Gray" FontSize="22" VerticalAlignment="Bottom" />
                                                                        <TextBlock Text="{Binding id}" FontSize="22" Foreground="Green" FontWeight="Bold" FontStyle="Italic" Margin="10,0,0,0" VerticalAlignment="Bottom" />
                                                                        <TextBlock Text=" item(s)" FontSize="22" Foreground="Silver" FontStyle="Italic" VerticalAlignment="Bottom" />
                                                                    </StackPanel>
                                                                </Expander.Header>
                                                                <ItemsPresenter />
                                                            </Expander>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </GroupStyle.ContainerStyle>
                                    </GroupStyle>
                                </ListView.GroupStyle>-->
                            </ListView>
                            <!--<TextBlock Text="{Binding Title}"></TextBlock>-->
                        <!--</StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>-->
            <StackPanel Grid.Column="1" Width="auto">
                <StackPanel Orientation="Horizontal">
                    <Label>Artist</Label>
                    <TextBox Width="147" Margin="5,0,0,0" Text="{Binding AlbumlistViewSource.CurrentItem.tunes.CurrentItem.artist}"></TextBox>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Label>Titre</Label>
                    <TextBox Width="147" Margin="10,0,0,0"  Text="{Binding AlbumlistViewSource.CurrentItem.tunes.CurrentItem.title}"></TextBox>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Label>Durée</Label>
                    <TextBox Width="147" Margin="2,0,0,0" Text="{Binding AlbumlistViewSource.CurrentItem.tunes.CurrentItem.length}"></TextBox>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Label>Année</Label>
                    <TextBox Width="147" Text="{Binding AlbumlistViewSource.CurrentItem.tunes.CurrentItem.year}"></TextBox>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Label>Genre</Label>
                    <TextBox Width="147" Margin="2,0,0,0" Text="{Binding AlbumlistViewSource.CurrentItem.tunes.CurrentItem.genre}"></TextBox>
                </StackPanel>

                <ListBox ItemsSource="{Binding AlbumlistViewSource.CurrentItem.tunes}" Margin="10,10,10,10" Height="250" Width="Auto" HorizontalAlignment="Stretch" IsSynchronizedWithCurrentItem="True">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding title}"></TextBlock>
                                <!--<TextBlock Text="{Binding artist}" Margin="10,0,0,0"></TextBlock>
                                <TextBlock Text="{Binding genre}" Margin="10,0,0,0"></TextBlock>
                                <TextBlock Text="{Binding length}" Margin="10,0,0,0"></TextBlock>
                                <TextBlock Text="{Binding year}" Margin="10,0,0,0"></TextBlock>-->
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </StackPanel>
            <StackPanel Grid.Column="2" Grid.Row="1">
                <StackPanel Orientation="Horizontal" >
                    <Image Source="{Binding AlbumlistViewSource.CurrentItem.album_cover}" Width="200" Stretch="Fill" Margin="5,0,0,0"></Image>
                    <StackPanel Orientation="Vertical">
                        <Button x:Name="AddTune" Content="Add Tune" Height="40" Width="75" Margin="75,10,0,0" Command="{Binding AddTuneCommand}"></Button>
                        <Button x:Name="DeleteTune" Content="Delete Tune" Height="40" Width="75" Margin="75,5,0,0" Command="{Binding DeleteTuneCommand}"></Button>
                        <Button x:Name="AddAlbum" Content="Add Album" Height="40" Width="75" Margin="75,5,0,0"  Command="{Binding AddAlbumCommand}"></Button>
                        <Button x:Name="DeleteAlbum" Content="Delete Album" Height="40" Width="75" Margin="75,5,0,0" Command="{Binding DeleteAlbumCommand}"></Button>
                    </StackPanel>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Label x:Name="SongTitle" Content="Title: " Foreground="Black" Margin="150,0,0,0"/>
                    <Label x:Name="SongTitleLabel" Height="30" VerticalAlignment="Top" HorizontalAlignment="Right" Content="{Binding AlbumlistViewSource.CurrentItem.tunes.CurrentItem.title, UpdateSourceTrigger=PropertyChanged}" Foreground="Black"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Label x:Name="SongGroup" Content="Group: " Foreground="Black" Margin="150,0,0,0"/>
                    <Label x:Name="SongGroupLabel" Height="30" VerticalAlignment="Top" HorizontalAlignment="Right" Content="{Binding AlbumlistViewSource.CurrentItem.tunes.CurrentItem.artist, UpdateSourceTrigger=PropertyChanged}" Foreground="Black"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Label x:Name="SongDuration" Content="Duration: " Foreground="Black" Margin="150,0,0,0"/>
                    <Label x:Name="SongDurationLabel" Height="30" VerticalAlignment="Top" HorizontalAlignment="Right" Content="{Binding AlbumlistViewSource.CurrentItem.tunes.CurrentItem.length, UpdateSourceTrigger=PropertyChanged}" Foreground="Black"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Label x:Name="SongYear" Content="Year: " Foreground="Black" Margin="150,0,0,0"/>
                    <Label x:Name="SongYearLabel" Height="30" VerticalAlignment="Top" HorizontalAlignment="Right" Content="{Binding AlbumlistViewSource.CurrentItem.tunes.CurrentItem.year, UpdateSourceTrigger=PropertyChanged}" Foreground="Black"/>
                </StackPanel>
                <ProgressBar x:Name="SongProgress" Height="10" IsIndeterminate="False"/>
                <Menu x:Name="SongButtonsMenu">
                    <Button x:Name="ShuffleButton" Content="Shuffle" Height="40" Width="45" Margin="0,5,5,0"  Command="{Binding ShuffleCommand}"/>
                    <Button x:Name="PlayButton" Content="Play" Height="40" Width="45" Margin="0,5,5,0"  Command="{Binding PlayCommand}"/>
                    <Button x:Name="PauseButton" Content="Pause" Height="40" Width="50" Margin="0,5,5,0"  Command="{Binding PauseCommand}"/>
                    <Button x:Name="NextButton" Content="Next" Height="40" Width="50" Margin="0,5,5,0"  Command="{Binding NextCommand}"/>
                    <Button x:Name="PreviousButton" Content="Previous" Height="40" Width="50" Margin="0,5,5,0"  Command="{Binding PreviousCommand}"/>
                    <Button x:Name="LikeButton" Content="Like" Height="40" Width="45" Margin="0,5,5,0"  Command="{Binding LikeCommand}"/>
                </Menu>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
