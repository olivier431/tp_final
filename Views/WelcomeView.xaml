<UserControl x:Class="tp_final.Views.WelcomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:tp_final.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0">Welcome to music application created by : Olivier Lefebvre, Félix Carignan, Samuel Cossette et Keven Kègle</Label>
        <Button Grid.Row="1" x:Name="Login" Content="Go to Login" Command="{Binding GoToLoginCommand}" />
    </Grid>
</UserControl>
